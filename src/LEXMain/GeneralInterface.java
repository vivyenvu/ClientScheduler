package LEXMain;

/**
 * Interface for lambda that adds a set amount of spaces between two strings. Used for the Customers per Country report.
 */
public interface GeneralInterface {
    String addSpace(String s, String t);
}
